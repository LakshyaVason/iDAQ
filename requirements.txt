# Core ML / LLM dependencies
transformers>=4.44.0
accelerate>=0.33.0
sentencepiece
protobuf
python-dotenv>=1.0.0
safetensors

# ML + analytics
pandas
scikit-learn
joblib
numpy<2

# HTTP and templating utilities
requests
jinja2
python-multipart  # Needed for FastAPI file uploads

# Firebase integration
firebase-admin

# LangChain ecosystem and serving
langchain
langchain-community
langserve[all]

fastapi
uvicorn

# Additional dependencies for PDF ingestion and vector search
faiss-cpu
langchain-text-splitters

# Optional: Python wrapper for Ollama (not required if using direct HTTP)
ollama
